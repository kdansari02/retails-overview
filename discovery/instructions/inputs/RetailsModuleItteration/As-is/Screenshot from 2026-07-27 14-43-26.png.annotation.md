# Annotation — Screenshot 14-43-26

Mobile "Orders" tab. Header unchanged (logo/name + ⓘ). "ORDER HISTORY" section
label, then order cards: order id (e.g. PO-10234), date · item count, a status
pill colored by state (Delivered = green, Out for Delivery = blue, Processing
= amber), the order total, and a "View details ›" link that opens the Order
Detail sheet (see image.png.annotation.md). Bottom tab bar with Orders active.
