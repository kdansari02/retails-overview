# Annotation — Screenshot 14-44-49

Mobile "Review Order" bottom sheet, opened from the floating cart bar's
"Review Order" button (dimmed backdrop over Shop). Lists cart lines
(name × qty — line total), grand Total, then two footer buttons: "Back to
Shop" (secondary, returns to Shop without clearing cart) and "Place Order"
(primary green — commits the order, clears the cart, and should surface the
new order at the top of Orders as "Processing").
