# Annotation — image.png

Mobile "Order Detail" bottom sheet, opened over the Orders list (dimmed backdrop).
Shows order id (PO-10234), date, status pill ("Delivered"), a 4-step vertical
timeline (Order Placed → Packed → Out for Delivery → Delivered, all green when
complete), itemized lines with qty × unit and line total, grand Total, and two
footer buttons: "Close" (secondary) and "Invoice · Reorder" (primary green).
